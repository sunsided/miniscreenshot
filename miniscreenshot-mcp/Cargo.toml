[package]
name = "miniscreenshot-mcp"
version = "0.3.0"
edition = "2021"
description = "MCP server exposing miniscreenshot Capture over streamable HTTP."
authors = ["Markus Mayer <widemeadows@gmail.com>"]
license = "EUPL-1.2 OR MIT OR Apache-2.0"
repository = "https://github.com/sunsided/miniscreenshot"
keywords = ["screenshot", "mcp", "model-context-protocol"]
categories = ["multimedia::images", "graphics"]
readme = "../README.md"

[dependencies]
miniscreenshot = { version = "0.3", path = "../miniscreenshot" }

rmcp = { version = "1.7", default-features = false, features = [
    "server",
    "macros",
    "schemars",
    "transport-streamable-http-server",
] }

axum = { version = "0.8", features = ["tokio"] }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "signal", "net"] }
serde = { version = "1", features = ["derive"] }
schemars = "1"
thiserror = "2"
base64 = "0.22"

[dev-dependencies]
miniscreenshot-desktop = { version = "0.3", path = "../miniscreenshot-desktop" }
miniscreenshot-portal = { version = "0.3", path = "../miniscreenshot-portal", default-features = false, features = ["tokio"] }
miniscreenshot-wgpu = { version = "0.3", path = "../miniscreenshot-wgpu" }
tempfile = "3"
png = "0.18"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

[features]
default = []
